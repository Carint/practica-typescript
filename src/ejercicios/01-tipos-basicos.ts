/*
    ===== Código de TypeScript =====
*/

// Declaración de una variable
let nombre: string = 'Carlos';
let mayorEdad: boolean = true;

// Variable que puede recibir otro tipo de dato
let nivelCarga: number | string = 95;
nivelCarga = 'Carga completa';

// Salida de datos en console:
console.log(nombre, mayorEdad, nivelCarga);